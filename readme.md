Курсовой проект

1) В kernel/rasterization есть два отдельных файла, первый из которых алгоритмический, и, соответственно, он оперирует только алгоритмами, которые не привязаны к библиотеке (Qt, в данном случае), а второй - уже rasterization .cpp/h, заточенный для отрисовки конкретно в Qt